# Criterios de aceptacion · H1.14 · Ingesta reejecutable con cadencia declarada por evento y producto declarado

**Fecha.** 2026-09-03
**Responsable.** Alejandro, por **D-38** (la historia era de Cesar, que la renombro el 2026-08-27)
**Epica E1 · 5 puntos · 7.8 h estimadas · Sprint S3**
**Materia y criterio de rubrica.** BD-1
**Depende de.** H1.1, H1.2 (cerradas). Se apoya en H6.3 (fabrica de fuentes) y se coordina con H12.1 (bitacora, Luna)
**Bloquea a.** Nada directo. Es lo que le daria «hoy» a un modelo que necesite la matriz (D-39)
**Estado.** Propuesta. Se escriben **antes** de tocar ningun archivo.

---

## Que hace esta historia, y que no

Hoy el sistema es una foto: H1.1 y H1.2 descargaron 35 anios una vez y nadie
vuelve a consultar las fuentes. Esta historia entrega **un guion reejecutable**,
`python -m backend.etl.ingestar`, que trae lo nuevo de cada fuente con la
cadencia que D-26 declaro, deja escrito **que producto** cargo, no duplica
nada si se corre dos veces, y registra cada corrida.

Lo que Cesar dejo escrito al renombrarla, y que sigue en pie: **no resuelve
donde se ejecuta**. No hay entorno alojado; la programacion queda declarada y
sin destino, que es «un guion con un cron escrito al lado». Y por **D-31**, no
regenera el manifiesto de H1.7: emite su recibo de carga y termina.

---

## Lo que ya esta decidido y NO se decide aca

| Decision | Donde |
|---|---|
| La cadencia por evento: incendio diaria, lluvia diaria con producto declarado, sequia semanal como mucho | **D-26** |
| El producto se declara: preliminar y final no son el mismo dato | **D-17** (precipitacion), **D-25** (era de FIRMS) |
| El manifiesto del dataset se corta a mano; la ingesta no lo toca | **D-31** |
| La ausencia es `None`; una fila repetida no puede esconder un dia ausente | **D-07**, hallazgo de Luna en H1.5 |
| Las fuentes se construyen por la fabrica, no a mano | **H6.3** |
| Los fallos de escritura van a `control.fallo` con la corrida que los produjo | H1.9, migracion 012 |

---

## Criterios

Los criterios 1 a 7 se comprueban con `python -m backend.etl.verificar_h1_14`,
**sin red y sin base**: fuentes falsas registradas en la fabrica de H6.3 y la
conexion falsa de las pruebas del repositorio. El 8 es una corrida real,
acotada, y va con su salida en la evidencia.

| # | Criterio | Como se comprueba |
|---|---|---|
| CA-1 | **La cadencia es un dato, no un comentario**: un diccionario por evento que el guion imprime al arrancar, y que dice `diaria` para incendio y lluvia intensa y `semanal` para sequia | Se lee el diccionario y se comprueba que sequia no es diaria y que los tres eventos estan |
| CA-2 | **La ventana de cada corrida sale de la cadencia y de la ultima corrida registrada**, no de una fecha escrita a mano: incendio y lluvia traen desde el ultimo dia cargado hasta ayer; sequia solo corre si pasaron 7 dias o mas desde su ultima corrida, y si no, lo dice y no hace nada | Con un registro falso que dice «ultima corrida hace 3 dias», sequia no corre e incendio si; con «hace 8 dias», corren los dos |
| CA-3 | **Cada fila declara su producto**: `fuente_precipitacion` distingue el CHIRPS preliminar del final, y el registro de la corrida dice cual trajo | Se inspecciona el SQL emitido contra la conexion falsa: las filas del preliminar llevan su codigo, las del final el suyo |
| CA-4 | **Idempotente**: dos corridas seguidas sobre la misma ventana no duplican ni cambian filas; la segunda queda registrada con cero filas nuevas | Con la fuente falsa devolviendo lo mismo dos veces, el segundo lote emite el mismo `INSERT ... ON CONFLICT` y el registro dice `filas=0` |
| CA-5 | **El final reemplaza al preliminar, y el preliminar nunca pisa al final**. Las filas de `analitico.riesgo` no se tocan: reemplazar un dato de entrada no reescribe una estimacion; recalcularla es volver a correr `estimar_riesgo`, que es idempotente (H3.6) | Dos lotes sobre el mismo dia en orden preliminar→final actualizan; en orden final→preliminar no. Se lee del SQL, con la condicion escrita |
| CA-6 | **Cada corrida queda registrada**: proceso, ventana, producto, filas, inicio, fin y estado, en la bitacora de H12.1 (`control.bitacora_etl`). Una corrida que falla queda registrada como fallida, con el motivo, y declara `geoguardian.corrida_id` en la sesion para que `control.fallo` la enlace (migracion 012) | Contra la conexion falsa: el `INSERT` de apertura, el `SET LOCAL`, y el `UPDATE` de cierre con el estado correcto en los dos caminos, exito y fallo |
| CA-7 | **Sin red no se inventa nada**: si `disponible()` de la fuente dice que no, la corrida se registra como fallida y no escribe una sola fila | Fuente falsa con `disponible() = False`: cero `executemany`, un registro con estado `fallida` |
| CA-8 | **Corrida real, acotada**: `ingestar --evento incendio` y `--evento lluvia_intensa` contra FIRMS y CHIRPS, sobre la ventana que la cadencia decida, con `contar` antes y despues; y una segunda corrida inmediata que no cambia nada | Salida de las dos corridas en la evidencia, con los conteos de `crudo.medicion_diaria` y `crudo.foco_calor` |

**El control se prueba rompiendolo**: se sabotea la condicion de reemplazo
(CA-5), la cadencia de sequia (CA-2) y el registro del fallo (CA-6), y se
comprueba que cae el criterio correcto.

---

## Dos cosas que hay que resolver antes de escribir, y con quien

1. **La bitacora es de H12.1, que es de Luna y arranca esta semana.** H1.14 no
   crea una segunda tabla de corridas: escribe en `control.bitacora_etl`. Para
   que las dos historias no se pisen, se acuerda con Luna la forma minima que
   H1.14 necesita (proceso, ventana, producto, filas, inicio, fin, estado,
   mensaje) y H12.1 la trae con el resto de lo suyo. Si H12.1 no esta lista
   cuando H1.14 llegue a CA-6, H1.14 trae esa forma minima en su propia
   migracion y H12.1 la extiende; queda dicho en la nota a Luna.
2. **El producto preliminar de CHIRPS en ClimateSERV.** H1.1 usa el tipo de
   dato `0`; que numero de catalogo corresponde al preliminar, y si el servicio
   lo sirve, **se comprueba contra el catalogo real antes de escribir el
   extractor**, no se supone. Si no lo sirve, lluvia intensa se declara con el
   producto que si hay y la cadencia diaria queda con la latencia real de ese
   producto, escrita.

---

## Lo que NO cierra esta historia

- Donde corre: alojamiento y programacion (fuera del alcance, como dijo Cesar).
- `/salud.ultima_ingesta`: la bitacora deja el dato listo; leerlo desde la API
  es `rutas.py`, de Cesar, y va con I-30.
- Regenerar la matriz de caracteristicas y recalcular el riesgo despues de
  ingerir: son `generar_caracteristicas` y `estimar_riesgo`, que ya existen y
  son idempotentes. La evidencia deja el orden escrito.
