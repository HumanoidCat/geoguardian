# Criterios de aceptacion · H3.6 · Cierre: la tabla decide y la tuberia escribe

**Fecha.** 2026-09-03
**Responsable.** Alejandro
**Epica E3 · 10 puntos · 15.6 h estimadas · Sprint S3**
**Materia y criterio de rubrica.** OE2
**Depende de.** H3.3, H3.4, H3.5, cerradas
**Bloquea a.** H3.7, H3.8, H4.1, H4.4
**Estado.** Propuesta. Se escriben **antes** de tocar ningun archivo.

---

## Que cierra esta historia, y que ya estaba cerrado

El arnes y el contrato de la tabla se entregaron el 2026-08-27, con sus siete
criterios CA-1 a CA-7 en `H3.6-tabla-comparativa.md` y `verificar_h36.py` en el
CI. **No se marco `[x]`** porque decia «tres algoritmos» y no existia ninguno.

Hoy existen los tres. Lo que falta para cerrarla es lo que la evidencia de H3.3
dejo escrito: *«decidir que modelo escribe es H3.6»*. Es decir:

1. **Una regla que decide con la tabla** -D-39-, y no un nombre a mano.
2. **La tuberia**: tres metodos del repositorio contra `analitico.riesgo`, y un
   guion que corre la tabla, elige, ajusta el escritor con todo el dato y
   escribe una fila por distrito, dia y evento.
3. **El visor en modo `postgres`** sirviendo esas filas, y `Salud.modo` dejando
   de decir `simulado`.

Lo que **no** entra: `guardar_metricas` y `listar_metricas` (pasan a H3.7 con la
tabla `analitico.metrica`), la explicacion SHAP (H4.2), y el cluster desplegado,
que no tiene datos cargados y seguira en `simulado` hasta que los tenga.

---

## Criterios

Los criterios 8 a 15 se comprueban con `python -m backend.modelado.verificar_h36_tuberia`,
sin base ni CSV real; el 16 y el 17, contra la base levantada, y van con su
salida en la evidencia.

| # | Criterio | Como se comprueba |
|---|---|---|
| CA-8 | La regla de D-39 elige al ganador cuando gana **fuera del ruido** | Tabla fabricada con ventaja > rango: `elegir_escritor` devuelve al primero |
| CA-9 | En empate tecnico elige **al mas simple** de los que estan dentro del ruido del primero | Tabla fabricada donde el primero es xgboost con rango ancho: devuelve `climatologica` |
| CA-10 | **La trivial nunca escribe**, ni siendo primera | Tabla donde la trivial es primera: devuelve al siguiente elegible o a nadie |
| CA-11 | **Nadie escribe por debajo del piso**: si ningun elegible alcanza la media de la trivial, devuelve `None` con motivo | Tabla con la climatologica bajo la trivial: `None`, y el motivo nombra el piso |
| CA-12 | La regla es la de CA-5, no otra: «fuera del ruido» significa ventaja > rango del primero, ni mas ni menos | Ventaja igual al rango cuenta como empate |
| CA-13 | Cada escritor entrega **P(nivel = alto)** por D-21: regresion y climatologica tienen `probabilidades()`, coherente con `predecir()` fila a fila, y `None` donde no se predice | Igual que el criterio 11 de H3.4, sobre los dos estimadores que no lo tenian |
| CA-14 | La probabilidad de la climatologica es la **tasa de ALTO** en ese distrito y mes en el entrenamiento | Se ajusta con datos construidos donde la tasa se conoce, y coincide |
| CA-15 | Los tres metodos del repositorio emiten el SQL correcto contra una conexion falsa: `guardar_riesgos` inserta con `ON CONFLICT` sobre la clave natural y dentro de una transaccion; `obtener_riesgo` devuelve `None` sin fila; `obtener_riesgos_por_fecha` devuelve **un `Riesgo` por distrito**, con `nivel` `None` donde no hay fila | Conexion falsa de `test_repositorio_postgres.py`; se cuentan eventos y se leen parametros |
| CA-16 | El guion escribe filas reales: `algoritmo` y `version_modelo` declarados, `probabilidad` en [0, 1], ninguna fila de sequia, y **es idempotente**: correrlo dos veces no cambia ninguna fila ni dispara la auditoria de H1.13 | Corrida real, `SELECT count(*)` antes y despues, y `analitico.riesgo_auditoria` sin filas nuevas en la segunda |
| CA-17 | El visor en modo `postgres`: `GET /salud` dice `real`, y `GET /riesgos?fecha=<ultimo dia con datos>&tipo_evento=incendio` trae ocho distritos con nivel y probabilidad, con `version_modelo` que nombra al escritor | `docker compose` con `GEOGUARDIAN_REPOSITORIO=postgres` y `curl` |

**El control se prueba rompiendolo**: se sabotea cada rama de la regla
(ganador dentro del ruido, trivial escribiendo, escribir bajo el piso) y se
comprueba que cae el criterio correcto y solo ese. Queda en la evidencia.

---

## Lo que ya esta decidido y NO se decide aca

| Decision | Donde |
|---|---|
| Que estimador escribe, y con que regla | **D-39** |
| `probabilidad` es P(nivel = alto) | **D-21** |
| La ausencia es `None`, nunca riesgo bajo | **D-07** |
| La sequia no es modelable | **D-34** |
| El simulado sigue siendo el valor por omision de la API | H6.2, `dependencias.py`; se cambia por variable de entorno |
