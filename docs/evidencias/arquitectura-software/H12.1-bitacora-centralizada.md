# H12.1 · Centralizar los logs de pipeline y aplicación en `control.bitacora_etl`

**Fecha.** 2026-09-04
**Responsable.** Luis Alejandro Luna García (Luna)
**Traspasada desde César** el 2026-09-03 por **D-37**
**Rúbrica.** Troubleshoot · **Épica** E12 · **Bloquea a** H12.2 y H12.4
**Excepción de propiedad.** `docs/07-propiedad-archivos.md`, sección «Excepción:
H12.1, para Luna». Alcance: `basedatos/`, y solo lo que esta historia necesita.

Criterios en `H12.1-criterios-aceptacion.md`, reescritos el 2026-09-04.

---

## Qué se entrega

| Archivo | Qué es |
|---|---|
| `basedatos/ddl/014_bitacora_etl_diagnostico.sql` | La migración. **Extiende**, no crea |
| `basedatos/verificar_h12_1.py` | 24 comprobaciones sobre 11 de los 12 criterios, y la tercera medición de D-44 |
| `basedatos/seguridad/verificar_h18.py` | 5 casos nuevos, que son las dos primeras mediciones de D-44 |
| `docs/03-bitacora-decisiones.md` | **D-44**, la decisión sobre el `GRANT` |

La tabla ya existía. La creó **H1.14** en la migración `013`, en su forma mínima,
el mismo día en que se escribieron los primeros criterios de esta historia. El
alcance pasó de crear a extender, y el porqué está en el documento de criterios.

Lo que la `014` agrega: cuatro columnas (`sqlstate`, `filas_leidas`,
`version_codigo`, `reportado_por`), el estado `parcial`, dos restricciones de
coherencia, la clave foránea que la `012` había prometido, el índice parcial del
detector de procesos muertos, y el permiso de escritura para `geoguardian_api`.

## Cómo se verifica

```powershell
docker compose up -d
docker compose ps                      # esperar a que geoguardian-db diga healthy
python -m basedatos.aplicar_migraciones
python -m basedatos.verificar_h12_1
```

Resultado del 2026-09-06: **24 de 24 comprobaciones**, un hallazgo declarado.

El verificador escribe todo dentro de un `SAVEPOINT` y cierra con `ROLLBACK`. No
deja filas en la base.

---

## Hallazgos

### 1. La tabla era nueva, así que las restricciones `NOT VALID` no comprobaron nada

`control.bitacora_etl` tenía **0 filas** y `control.fallo` también. Los tres
conteos de violaciones dieron cero.

**Eso no es «los datos están limpios», es «no había datos».** Son cosas distintas
y se registran distintas.

> **Corrección del 2026-09-05.** La primera versión de este hallazgo decía que
> «la base local estaba vacía» y que se había levantado de cero ese día. **Es
> falso, y nunca se midió**: se infirió de que esta tabla y `control.fallo`
> estaban vacías. Medido después, `crudo.medicion_diaria` tenía **104 240 filas
> desde 1991-01-01**.
>
> La razón real es otra y es más precisa: **`control.bitacora_etl` la creó la
> migración `013` el día anterior**, así que no podía tener filas viejas. Una
> tabla recién creada y una base vacía se ven idénticas desde adentro de esa
> tabla, y esa es justamente la confusión en que se cayó.
>
> La conclusión operativa no cambia —las restricciones `NOT VALID` siguen sin
> ejercerse— pero la razón importa: no hay que esperar a que «se cargue la base»,
> hay que esperar a que **esta tabla acumule corridas**.
>
> No se borra porque es una **inferencia presentada como una medición**, que es
> el error que este proyecto persigue en los datos y en los modelos. El mismo
> error estaba en los criterios de H4.1 y también se corrigió allí.

Las tres restricciones entraron `NOT VALID` a propósito, para que unas filas
heredadas incoherentes fueran un hallazgo sobre los datos y no un error de
despliegue. Esa decisión sigue siendo la correcta, pero **todavía no se ejerció**:
no hubo filas viejas contra las cuales ejercerla. Queda pendiente volver a correr
el informe de estado del verificador cuando la tabla tenga corridas reales
acumuladas, que es cuando la pregunta empieza a tener respuesta.

**Primera corrida real acumulada:** la ingesta del 2026-09-04 dejó una fila
(`id = 39`, `exitosa`). Una fila no ejerce nada todavía, pero el contador ya
arrancó.

`bitacora_etl_fin_coherente_ck`, `bitacora_etl_orden_temporal_ck` y
`fallo_corrida_fk` siguen sin validar contra filas anteriores. `NOT VALID` no
significa apagada: muerde en todo lo que entre desde ahora, y el verificador lo
comprueba en los criterios 4, 5 y 7.

### 2. La migración no era idempotente, y el aplicador lo estaba tapando

El criterio 3 pide que la migración se aplique dos veces sin error. Correr
`aplicar_migraciones` dos veces **no lo comprueba**: el aplicador guarda la suma
SHA-256 de cada archivo y nunca reaplica uno ya aplicado. Lo que se demuestra así
es que el aplicador es idempotente, no que el archivo lo sea.

Al escribir el verificador se ejecutó el contenido del archivo dos veces de
verdad, y apareció: `bitacora_etl_leidas_ck` era la única de las cinco
restricciones sin su `DROP CONSTRAINT IF EXISTS` delante, y PostgreSQL **no
acepta `ADD CONSTRAINT IF NOT EXISTS`**. La segunda pasada habría fallado con
«la restricción ya existe».

Corregido. La migración se desregistró de `control.migracion` y se volvió a
aplicar, lo cual es legítimo porque todavía no estaba en `dev` y solo existía en
una máquina. **La regla de que una migración aplicada no se edita no se saltó:**
se deshizo la aplicación primero.

Lo que este hallazgo enseña es más general que el defecto: **una herramienta que
protege contra un error también puede esconderlo.** Si el verificador se hubiera
limitado a llamar al aplicador dos veces, habría dado verde sobre un archivo roto.

### 3. El criterio 1 decía «cinco columnas» y son cuatro

Contaba el estado `parcial` como una columna más. `parcial` no es una columna: es
un valor que el `CHECK` de una columna existente pasa a admitir. La tabla de
faltantes del propio documento de criterios siempre listó cuatro.

**Se corrigió el criterio, no el código.** El código estaba bien; el criterio
contaba mal.

### 4. El plan del criterio 8 no es evidencia de rendimiento, en ninguna dirección

El índice parcial existe y el planificador puede usarlo: con `enable_seqscan =
off` el plan lo elige. Sin forzar nada, sobre las pocas filas que el verificador
inserta, **también lo eligió**.

Eso no demuestra que sirva. Con una tabla prácticamente vacía el planificador
tiene poco que comparar, y el resultado no es información en ninguno de los dos
sentidos.

La primera versión del verificador solo avisaba en el caso desfavorable —si el
índice **no** se elegía—, lo que convertía el caso contrario en una victoria
silenciosa. Se corrigió para que avise en los dos. **Un criterio que solo se
queja cuando el resultado incomoda no es un criterio, es una hinchada.**

Lo demostrado es: el índice existe, y el planificador puede usarlo. Que valga la
pena se vuelve a medir cuando haya volumen.

---

## D-44 · El `GRANT` que esta historia concede, y por qué necesitaba una decisión

La `014` le da a `geoguardian_api` **`INSERT` y `UPDATE` sobre
`control.bitacora_etl`**, y solo eso, porque el título de la historia dice
«pipeline **y aplicación**». Con eso **`geoguardian_api` deja de ser un rol de
solo lectura**, que es una propiedad afirmada por escrito en tres lugares del
repositorio.

La decisión completa está en **D-44**. Lo que importa registrar aquí es por qué
no bastaba con corregir una línea de documentación:

**Las tres afirmaciones tienen controles automáticos detrás, y los tres siguieron
en verde después de la `014`**, porque cada uno prueba una tabla distinta de la
que se abrió. CA-6 de H11.6 intenta un `INSERT` en `analitico.riesgo`; el caso de
H1.8 intenta un `UPDATE` en `control.migracion`. Ninguno mira `bitacora_etl`.

La frase quedó falsa y el conjunto de controles no se enteró. Eso es lo que las
mediciones de abajo cierran.

### Las tres mediciones, y por qué ninguna sobra

| # | Dónde | Qué comprueba |
|---|---|---|
| 1 | `verificar_h18.py`, `PERMITIDAS` | La API **puede** abrir una corrida con `INSERT` y cerrarla con `UPDATE` |
| 2 | `verificar_h18.py`, `PROHIBIDAS` | **Sigue sin poder** `DELETE` en `bitacora_etl` ni `INSERT` en `analitico.riesgo` |
| 3 | `verificar_h12_1.py`, caso 9b | El segundo proceso no le cambia el significado a `ultima_ingesta` |

**La primera sola probaría que se concedió un permiso, y nada más.** Es la
segunda la que distingue «se abrió una tabla» de «se abrió la base», y por eso el
par va junto o no va.

Se agregó además un quinto caso que nadie pidió: que **`geoguardian_etl` tampoco
pueda borrar** en `bitacora_etl`. La `013` le dio `SELECT, INSERT, UPDATE` y nunca
`DELETE`, así que la propiedad ya existía y no la comprobaba nadie.

Corrida del 2026-09-06: los cinco en verde y **con el motivo correcto** —
`permission denied for table bitacora_etl` y `permission denied for table riesgo`,
no un error distinto que los dejara pasar.

### La tercera medición, que es la que menos se ve venir

`/salud` responde `ultima_ingesta` con
`max(terminada_en) ... WHERE estado = 'exitosa' AND proceso LIKE 'ingesta.%'`.

**Ese filtro existe por esta historia.** El día que la API escriba con
`proceso = 'api'`, sin él el campo pasaría a significar «la última vez que la API
se anotó a sí misma» **sin que nadie tocara el contrato**. `verificar_h61.py` no
puede probarlo: corre sin base a propósito, y hacen falta filas de dos procesos
distintos.

El caso 9b comprueba las dos mitades: que con el filtro devuelve la ingesta, y
—la que vale— que **sin el filtro devolvería otra cosa**. Si las dos dieran igual,
la prueba estaría pasando sin ejercer nada.

### Un fallo de la propia prueba, y qué lo atrapó

La primera versión del caso 9b envejecía la corrida de ingesta moviendo solo
`terminada_en`. **`bitacora_etl_orden_temporal_ck` la rechazó por duración
negativa** — la restricción que esta misma migración agrega, atrapando una corrida
imposible escrita por su propio verificador. Se corrigió moviendo las dos fechas.

### Lo que esta historia toca fuera de su carpeta

`basedatos/seguridad/verificar_h18.py` es de César. Se toca bajo la excepción
estrecha de H12.1 y **solo para comprobar el `GRANT` que esta historia
introduce**. El cambio incluye una parte que no era estrictamente necesaria y se
declara: la lista `PERMITIDAS` marcaba las escrituras poniendo `None` en la
consulta, y el probador llevaba dentro, escrito a mano, el único `INSERT` que
sabía hacer. **Un centinela que solo puede significar una cosa deja de servir en
cuanto hay dos.** Se reemplazó por un cuarto elemento explícito.

### Tres documentos que quedan desactualizados, y no se corrigen aquí

| Documento | Qué pasa |
|---|---|
| `docs/19-runbook-railway.md` | Dice «rol de **solo lectura**». **Falso desde la `014`** |
| `H6.1-api-rest-openapi.md` | Cierto sobre los **endpoints**, falso sobre el **rol**. Son dos cosas que hoy se escriben igual |
| `H11.6-criterios-aceptacion.md`, CA-6 | **No cambia**: lo que prueba sigue siendo verdad |

Tocan carpetas de evidencia de H6.1 y H11.6 y no son de H12.1. Se declaran para
que consten identificadas, y se avisan en el Pull Request.

---

## Desviaciones declaradas frente a `docs/investigacion/requisitos-bitacora-h12.4.md`

Ese documento se escribió el 1 de septiembre, desde el lado del consumidor, antes
de saber cómo iba a quedar la tabla. **No se ajusta.** Su valor está en haberse
escrito antes, y ajustarlo ahora para que calce con lo construido lo destruiría.

| Pedía | Qué se hizo | Por qué |
|---|---|---|
| `filas_leidas` y `filas_escritas` | Solo `filas_leidas` | `filas` ya guarda las escritas. Dos columnas para el mismo dato es la incidencia **I-07**: un día una dice una cosa y la otra otra |
| `parametros jsonb` genérico | No se agrega | `ventana_desde`, `ventana_hasta` y `producto` ya son columnas tipadas, que el motor valida. Un `jsonb` al lado sería un segundo lugar para lo mismo con menos garantías |

Los tres números que H12.4 necesita quedan así: leídas en `filas_leidas`,
escritas en `filas`, rechazadas contadas desde `control.fallo` por `corrida_id`
—posible gracias a la clave foránea que esta historia agrega—.

## Lo que esta historia no hace

- **No implementa el diagnóstico.** Eso es H12.4.
- **No lee la bitácora desde un contrato.** Eso es SC-08.
- **No migra las corridas viejas.** Las anteriores quedan con `sqlstate` y
  `filas_leidas` en `NULL`. Inventarles un valor sería falsear el registro.
- **`parcial` se agrega y todavía nadie lo escribe.** Es correcto por ahora —lo
  usará quien cargue por distrito— pero queda dicho, para que no parezca en uso.

## Una nota sobre el control cruzado, que sigue en pie

Con el traspaso, el que produce la bitácora (H12.1) y el que la consume (H12.4)
pasaron a ser la misma persona. La condición que el PM puso al aprobar **SC-08**
—que César firmara el contrato antes de que yo escribiera contra él— **se
disolvió con el traspaso**, y con ella el control cruzado.

No hay conflicto, y tampoco hay una segunda mirada. Queda escrito para que el PM
decida si quiere que otra persona revise.

---

## Criterio 10, que es el que más importa

Los once criterios comprobables se pueden cumplir con una migración perfecta que
rompa el ETL de otro. `control.bitacora_etl` la usa `backend/etl/ingestar.py`, de
H1.14, que corre contra datos reales.

```powershell
python -m backend.etl.ingestar --evento lluvia_intensa
```

**Corrida real del 2026-09-04, 22:17:52 a 22:18:48 (hora de Costa Rica).** El ETL
corrió entero después de la extensión: 8 distritos, 56,4 s, **0 procesos
fallidos**, 1968 filas escritas. La extensión no rompió el trabajo de H1.14.

La fila que dejó en `control.bitacora_etl`:

```
id             39
proceso        ingesta.lluvia_intensa
estado         exitosa
iniciada_en    2026-09-04 22:17:52.662701-06:00
terminada_en   2026-09-04 22:18:48.866506-06:00
ventana_desde  2025-12-29
ventana_hasta  2026-09-03
producto       chirps
filas          1968
filas_leidas   None
sqlstate       None
version_codigo None
reportado_por  None
```

Con eso quedan comprobados los doce criterios.

### Las cuatro columnas nuevas salieron en `NULL`, y es correcto

`filas_leidas`, `sqlstate`, `version_codigo` y `reportado_por` están vacías
porque quien las llenaría es `backend/etl/ingestar.py`, que **es de H1.14 y esta
historia no lo toca** —la excepción de `docs/07` es estrecha y termina donde
empieza el trabajo de otro—.

**Son columnas disponibles, no columnas en uso.** Queda dicho aquí para que
H12.4 no construya suponiendo que traen dato: hoy no lo traen, y el diagnóstico
tiene que tratarlas como ausentes hasta que alguien las llene. Llenarlas es una
línea en el cierre de corrida de `ingestar.py`, y es una solicitud de cambio a
nombre de H1.14, no un arreglo silencioso desde aquí.

Es el mismo caso que `parcial`: el estado existe y todavía nadie lo escribe.

### Por qué el `id` es 39 y no 1

La tabla tenía 0 filas y la primera corrida real quedó con `id = 39`. No falta
ninguna corrida: el verificador abre corridas de prueba dentro de un `SAVEPOINT`
y las deshace, pero **las secuencias de PostgreSQL no se revierten con el
`ROLLBACK`**, por diseño, para que dos transacciones simultáneas nunca reciban el
mismo número.

Se escribe porque quien diagnostique dentro de tres meses va a ver el hueco de 1
a 38 y merece encontrar la explicación aquí, en vez de abrir una incidencia por
38 corridas que nunca existieron.

---

## Una observación que sale de esta corrida y NO es de esta historia

La ventana pedida fue de **249 días** y CHIRPS devolvió **3 días por distrito**:

```
  ventana 2025-12-29 a 2026-09-03 (249 dias)
  Tilarán: CHIRPS 3 dias, 0 sin dato
  ...
  1992 mediciones, 1968 sin precipitacion
```

1992 = 249 × 8 distritos. 1968 = 1992 − 24, es decir 3 días con precipitación por
distrito. **El 98,8 % de las filas entró sin valor de precipitación.**

D-40 midió la latencia de CHIRPS en 33 días, así que sobre 249 días deberían
haber llegado unos 216. La diferencia es demasiado grande para explicarse por
latencia, y **no se afirma aquí cuál es la causa porque todavía no se midió.**

Hay un segundo detalle que conviene mirar al mismo tiempo: `filas` quedó en 1968,
que es exactamente el mismo número que el registro llama «sin precipitación».
Puede ser coincidencia. Puede ser el mismo número contando dos cosas distintas,
que es la forma de la incidencia **I-07**.

**Y esto es justamente para lo que se agregó `filas_leidas`.** Sin ella no se
puede distinguir «la fuente trajo 1992 y se escribieron 1968» de «se leyeron 1968
y se escribieron 1968». La columna ya existe; falta que alguien la llene, y
mientras tanto la pregunta se responde a mano.

Se anota aquí, se investiga aparte y se abre incidencia solo con el número
delante. Corresponde a calidad de datos —H1.5— y toca `backend/etl`, que es de
Alejandro.
