# Evidencia · H11.6 · Publicar la API y la base, y que el visor sirva dato real

**Fecha.** 2026-09-05. **Responsable.** Alejandro.
**Criterios.** `docs/evidencias/sistemas-operativos/H11.6-criterios-aceptacion.md`,
escritos el 2026-09-04 **antes** de tocar ningun archivo y antes de crear nada en
Railway.
**Decision.** D-43. **Incidencias.** I-35, I-36.
**Procedimiento.** `docs/19-runbook-railway.md`.

---

## Lo que hace esta historia

D-05 fue precisada el 2026-08-20 y dejo escrito que «produccion» era un espacio de
nombres en una laptop, que eso contradice el proposito del producto, y cual era el
trabajo pendiente y en que orden: **publicar la API y la base**, automatizar la
ingesta, y recien entonces retirar el aviso de datos simulados.

Esta historia hace el **paso 1**. La precondicion -que exista un modelo entrenado
escribiendo estimaciones- se cumple desde H3.6 y quedo afinada en H3.8.

---

## Dos cosas que no existian y sin las cuales nada de esto corre

Se encontraron **antes** de tocar Railway, leyendo el repositorio.

### 1. El arranque de la base solo existia dentro de docker-compose

Las cuatro extensiones y los cuatro esquemas los crea
`infra/docker/init-db/01-extensiones.sql`, que PostgreSQL ejecuta **solo cuando el
volumen esta vacio y solo desde docker-compose**. En cualquier base que no venga de
compose ese guion no existe, y la primera migracion cae:

```
Aplicando 001 001_control_migracion.sql ... FALLO
schema "control" does not exist
```

Comprobado el 2026-09-04 contra un PostgreSQL 16 recien creado, no supuesto.

`infra/preparar_base.py` aplica **ese mismo archivo leido del disco**, no una copia
de sus sentencias, y despues comprueba que las extensiones quedaron. El verificador
exige que sea asi: `ARRANQUE.read_text` tiene que estar en el codigo y no puede
haber ningun `CREATE` escrito dentro del guion.

### 2. La base tiene que llamarse `geoguardian`, y lo exige el DDL

Comprobado contra una base llamada `railway`:

```
Aplicando 003 003_seguridad_roles.sql ... FALLO
```

`basedatos/ddl/003_seguridad_roles.sql` nombra la base literalmente:

```sql
REVOKE ALL    ON DATABASE geoguardian FROM PUBLIC;
GRANT CONNECT ON DATABASE geoguardian TO geoguardian_etl, ...;
```

Es correcto -un `GRANT ON DATABASE` tiene que nombrarla- y **no se arregla
editando la migracion**: una migracion aplicada no se edita nunca, y el aplicador
compara su SHA-256 justamente para detectarlo.

`preparar_base.bases_que_exigen_las_migraciones()` **saca esa exigencia del DDL**,
no de una constante: recorre `basedatos/ddl/*.sql` buscando `ON DATABASE <nombre>`
y se planta citando el archivo y la linea. Si manana una migracion nombrara otra
base, el guion lo diria solo.

---

## CA-3 · El esquema se aplica, no se improvisa

Contra la base de Railway, `postgis/postgis:16-3.5`:

```
Base conectada
  base:         geoguardian
  servidor:     PostgreSQL 16.9 (Debian 16.9-1.pgdg110+1)
  cotejo:       en_US.utf8 / en_US.utf8 · UTF8
  extensiones:  plpgsql 1.0
  esquemas:     (ninguno)
  migraciones aplicadas antes: no
Aplicando infra\docker\init-db\01-extensiones.sql
  ok     CREATE EXTENSION IF NOT EXISTS postgis
  ok     CREATE EXTENSION IF NOT EXISTS postgis_raster
  ok     CREATE EXTENSION IF NOT EXISTS pg_stat_statements
  ok     CREATE SCHEMA IF NOT EXISTS geo
  ok     CREATE SCHEMA IF NOT EXISTS crudo
  ok     CREATE SCHEMA IF NOT EXISTS analitico
  ok     CREATE SCHEMA IF NOT EXISTS control
  ...
  ok     ALTER DATABASE "geoguardian" SET timezone TO 'America/Costa_Rica'
Preparada.
  extensiones:  pg_stat_statements 1.10, plpgsql 1.0, postgis 3.5.2, postgis_raster 3.5.2
  esquemas:     analitico, control, crudo, geo
```

```
Migraciones en disco:  13
Ya aplicadas:          0
Pendientes:            13
Aplicando 001 001_control_migracion.sql ... ok
Aplicando 002 002_geo_territorio.sql ... ok
Aplicando 003 003_seguridad_roles.sql ... ok
Aplicando 004 004_crudo_medicion.sql ... ok
Aplicando 005 005_crudo_foco_calor.sql ... ok
Aplicando 006 006_analitico_riesgo.sql ... ok
Aplicando 007 007_riesgo_correcciones.sql ... ok
Aplicando 008 008_auditoria_riesgo.sql ... ok
Aplicando 009 009_funciones_y_bitacora_fallos.sql ... ok
Aplicando 010 010_particionar_medicion.sql ... ok
Aplicando 011 011_indices.sql ... ok
Aplicando 012 012_corrida_en_la_bitacora_de_fallos.sql ... ok
Aplicando 013 013_ingesta_producto_y_bitacora.sql ... ok
Aplicadas 13 migraciones.
```

**PostGIS 3.5.2**, version publicada. Que diga `3.5.2` y no `3.7.0dev` costo la
incidencia **I-36**: la plantilla de Railway apunta a una etiqueta `-master`, que
es una construccion de la rama de desarrollo y no tiene numero de version que
citar.

---

## CA-6 · El rol de la API se probo intentando escribir

Contra la base de Railway, con `basedatos/seguridad/verificar_h18.py`:

```
  etl_geoguardian      creado       miembro de geoguardian_etl
  api_geoguardian      creado       miembro de geoguardian_api
Listos 2 usuarios. Ninguno tiene permisos propios.

CA-1 · Roles y usuarios con los atributos correctos ... CUMPLE
CA-2 · Pertenencia exacta a los roles ... CUMPLE
CA-3/4 · Lo permitido funciona ... CUMPLE
CA-5 · Toda operacion prohibida es rechazada ... CUMPLE
  [ok ] api  SELECT en crudo              permission denied for schema crudo
  [ok ] api  INSERT en geo.distrito       permission denied for table distrito
  [ok ] api  UPDATE en control.migracion  permission denied for table migracion
  [ok ] etl  INSERT en geo.distrito       permission denied for table distrito
  [ok ] etl  DROP TABLE en control        must be owner of table migracion
  [ok ] etl  DELETE en control.migracion  permission denied for table migracion
CA-6 · Permisos implicitos de PUBLIC retirados ... CUMPLE
CA-7 · Una tabla nueva nace con los permisos correctos ... CUMPLE
CA-8 · Ninguna contrasena en el repositorio ... CUMPLE
```

El criterio decia: **«un rol de lectura que nadie probo escribiendo no esta
probado»**. Esta probado, en la nube, y la salida esta arriba.

---

## CA-4 · El dato llega por un camino declarado y contado

*(pendiente: conteos de origen y destino)*

---

## CA-1, CA-2, CA-5, CA-7 · Contra el sitio publicado

*(pendiente)*

---

## CA-9 · El costo, medido a las 48 horas

*(pendiente: se mide el 2026-09-07, no el dia de la entrega)*

---

## CA-10 · La documentacion permite repetirlo sin mi

`docs/19-runbook-railway.md`, versionado. No es una guia escrita de memoria: trae
los cinco errores que se cometieron la primera vez, cada uno con la salida que lo
delato, porque eso es lo que de verdad le sirve a quien lo repita.

El verificador comprueba lo que se puede comprobar sin el despliegue: que el
runbook viva en `docs/` y no en `gestion/`, y que **nombre las siete variables de
entorno que el codigo lee de verdad** -sacadas de `basedatos/conexion.py` con una
expresion regular, no de una lista escrita a mano, para que envejecer no sea
gratis-.

---

## Lo que se rompio a proposito

`infra/verificar_h116.py` da **52 de 52** sin red, sin base y sin el despliegue
levantado. Se saboteo en nueve casos y **ninguno paso en verde**:

| # | Sabotaje | Que atrapo |
|---|---|---|
| 1 | `.gitignore` vuelve a `.env` exacto | `.env.railway` deja de estar ignorado |
| 2 | se «simplifica» a `.env*` | `.env.example` queda ignorado |
| 3 | el runbook se queda en `gestion/` | cinco comprobaciones de CA-10 |
| 4 | el runbook deja de nombrar `POSTGRES_PORT` | la comprobacion de variables |
| 5 | el runbook manda purgar antes de cambiar la imagen | la secuencia de I-36 |
| 6 | alguien pega un host real del proxy | el runbook deja de hablar por nombres |
| 7 | la lista de permanentes pierde `does not exist` | vuelve a esperar 90 s |
| 8 | la regla se hace **mas amplia**: cualquier `FATAL` | rompe el reintento de H1.3 |
| 9 | el dominio interno de Railway cuenta como local | manda a mirar `docker compose ps` |

**El caso 4 paso en verde al primer intento**, y no porque el control fallara: el
sabotaje se olvido de una cuarta aparicion de `POSTGRES_PORT`, dentro de un
`python -c`. Es la leccion de H3.4 otra vez -un sabotaje que pasa puede significar
que el sabotaje no rompia nada- y quedo anotada en el propio guion.

**El caso 8 es el que hay que mirar.** Sabotea la regla haciendola *mas amplia*,
que es la version que un revisor apurado aprobaria, y rompe
`FATAL: the database system is starting up` -el mensaje que PostgreSQL manda
mientras arranca, y el unico caso que el reintento existe para cubrir-.

---

## Lo que esta historia encontro en otras piezas

### I-35 · `.gitignore` filtraba `.env` exacto

`.gitignore` tenia la linea `.env`, que ignora **un archivo con ese nombre exacto**.
Un `.env.railway` con contrasenas reales **no estaba ignorado**. Corregido a
`.env.*` con `!.env.example`, con el motivo escrito en el propio archivo.

El control del verificador tambien estaba mal: preguntaba `".env" in texto` y
**respondia que si mientras el hueco existia**. Ahora aplica los patrones a nombres
concretos e incluye el caso que tiene que quedar **fuera**.

### I-36 · Se purgo el volumen antes de desplegar la imagen nueva

Un directorio de datos inicializado por una imagen y servido por otra:
`collation version mismatch, 2.41 contra 2.31`. El orden correcto -imagen,
desplegar, **despues** purgar- quedo escrito en el runbook.

Y dentro de esa incidencia, otra: el intento de arreglarlo con
`REFRESH COLLATION VERSION` devolvio `invalid collation version change`, **y se
leyo como que el camino no servia**. Los registros de despliegue de Railway traen
la sentencia exacta: fallo en `template0`, que no se toca por diseno y no hacia
falta. `template1` **si** se habia refrescado en ese mismo intento.

### El reintento de conexion esperaba por errores permanentes

`basedatos.conexion.conectar()` reintentaba noventa segundos ante cualquier
`OperationalError`, incluida `database "geoguardian" does not exist` -que el
servidor ya habia contestado-, y despues mandaba a mirar `docker compose ps` con
la base en otro continente.

**Medido:** en un fallo de conexion psycopg no expone el `sqlstate` -ni
`error.sqlstate` ni `error.diag.sqlstate`-, asi que `except OperationalError` no
puede separar «el servidor no esta» de «el servidor contesto que no».

La correccion vive bajo una **excepcion de propiedad escrita antes de tocar el
archivo** (`docs/07-propiedad-archivos.md`), estrecha: solo `conectar()` y solo
`.env.example`. La solicitud completa, con la medicion, esta en
`gestion/solicitud-cesar-reintento-conexion-2026-09-05.md` y se le manda a Cesar
igual: lo que cambia es el orden, no que se le consulte.

`infra/probar_reintento_conexion.py` da **5 de 5** contra un PostgreSQL 16 real,
comprobando clase de excepcion, tiempo y mensaje en cada caso.

**Al probarlo aparecio un defecto de mi propia correccion**: dejaba salir el
`OperationalError` crudo, y los modulos que llaman a `conectar()` capturan
`ErrorConexion` y nada mas. Habria cambiado un mensaje limpio por un rastro de
pila. Se envuelve, y el sabotaje 4 lo comprueba.

---

## Lo que NO cierra esta historia

- **La ingesta automatica.** Es el paso 3 de D-05 y va como historia aparte.
- **`base_datos_conectada`.** `rutas.py` lo devuelve `False` cableado y
  `verificar_h61.py` **exige** que sea `False`. Con la base publicada, la API dice
  `modo: real` y `base_datos_conectada: false` a la vez. El aviso del visor lo
  decide `modo`, asi que nada se rompe, pero **la API esta mintiendo sobre su
  propio estado**. Son dos archivos de Cesar: va como aviso, no como arreglo.
- **Los JSON de `frontend/public/simulados/`.** Siguen siendo la degradacion que
  exige la Definition of Done de H6.6, y el respaldo si Railway se cae.
- **Los manifiestos de Kubernetes.** No se tocan: son lo que evalua Arquitectura.
