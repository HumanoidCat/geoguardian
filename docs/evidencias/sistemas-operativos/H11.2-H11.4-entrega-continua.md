# Evidencia · H11.2, H11.3 y H11.4 · La cadena de entrega continua

**Fecha.** 2026-09-02
**Responsable.** Alejandro Josue Rodriguez Zamora
**Rubrica.** CICD
**Decision de arquitectura.** D-36

---

## Lo primero, porque cambia como se lee todo lo demas

**El flujo de despliegue NO despliega al cluster k3d del equipo.**

No puede: GitHub Actions corre en la nube y ese cluster vive en una maquina
detras de un router domestico, sin direccion publica. Lo que hace es **crear su
propio cluster k3d dentro del runner**, aplicar los mismos manifiestos,
comprobar que converjan, y destruirlo.

Esto no se descubrio al implementar: se decidio antes, con las alternativas
medidas, en **D-36**. Y lo que se pierde queda declarado alli y aca: **al
terminar no queda nada corriendo.**

El cluster persistente se actualiza cuando una persona corre
`infra/k8s/desplegar.py`, que aplica **exactamente los mismos manifiestos**.

---

## Lo que se entrega

| Archivo | Que es |
|---|---|
| `infra/k8s/base/api-deployment.yaml` · `api-service.yaml` | La API en Kubernetes |
| `infra/k8s/base/visor-deployment.yaml` · `visor-service.yaml` | El visor en Kubernetes |
| `infra/k8s/desplegar.py` | El guion de despliegue, **el mismo en CI y en local** |
| `.github/workflows/cd.yml` | Los tres entornos encadenados |
| `.github/acciones/preparar-cluster/action.yml` | El cluster efimero, una sola vez para los tres |
| `infra/verificar_cd.py` | 17 comprobaciones sin cluster, mas tres modos con cluster |

Los overlays de los tres entornos ya existian desde H8.6 y **no se tocaron**:
heredan la base, asi que la API y el visor entran solos en los tres.

---

## La comprobacion, y que distingue

    $ python infra/verificar_cd.py --manifiestos

      ok    la base incluye api-deployment.yaml
      ok    la base incluye api-service.yaml
      ok    la base incluye visor-deployment.yaml
      ok    el bloque images declara las dos imagenes de H11.1
      ok    el guion de despliegue sabe reemplazar la etiqueta
      ok    el guion espera a que converja: sin esto, `apply` siempre dice exito
      ok    api declara las dos sondas
      ok    api usa la imagen que publica H11.1
      ok    visor declara las dos sondas
      ok    visor usa la imagen que publica H11.1
      ok    DESTINO_API no termina en barra
      ok    DESTINO_API apunta al Service que existe, por su nombre real
      ok    el puerto de DESTINO_API es el del Service
      ok    el guion de despliegue no volvio a bash
      ok    el entorno desarrollo tiene su namespace y hereda la base
      ok    el entorno pruebas tiene su namespace y hereda la base
      ok    el entorno produccion tiene su namespace y hereda la base

    17 de 17 comprobaciones

### Se comprobo que el control DISTINGUE

Un control que no puede fallar no se distingue de uno que no comprueba nada. Es
la mitad que falto en **I-20**, asi que se introdujeron cuatro defectos a
proposito:

| Defecto introducido | Resultado |
|---|---|
| Barra final en `DESTINO_API` | **FALLA** «DESTINO_API no termina en barra» |
| Renombrar el Service a `api-backend` sin tocar `DESTINO_API` | **FALLA** «apunta al Service que existe» |
| Quitar el `rollout status` del guion | **FALLA** «el guion espera a que converja» |
| Quitar la sonda `readinessProbe` de la API | **FALLA** «api declara las dos sondas» |

Los cuatro se revirtieron y el verificador volvio a 17 de 17.

---

## La corrida contra un cluster de verdad

No alcanza con las 17 comprobaciones estaticas. **I-10** dejo escrito que un
verificador en verde dice que no fallo lo que se le pidio comprobar, no que el
resultado este bien. Asi que se desplego contra el k3d local, el 2026-09-02.

    $ python infra/k8s/desplegar.py desarrollo

    Desplegando a geoguardian-desarrollo con la etiqueta latest
      $ kubectl apply -k ...\k8s\local\desarrollo
    namespace/geoguardian-desarrollo unchanged
    configmap/postgis-init unchanged
    service/api created
    service/postgis unchanged
    service/visor created
    deployment.apps/api created
    deployment.apps/visor created
    statefulset.apps/postgis configured

    Esperando a que converja...
      $ kubectl -n geoguardian-desarrollo rollout status statefulset/postgis
    partitioned roll out complete: 1 new pods have been updated...
      $ kubectl -n geoguardian-desarrollo rollout status deployment/api
    Waiting for deployment "api" rollout to finish: 0 of 1 updated replicas are available...
    deployment "api" successfully rolled out
      $ kubectl -n geoguardian-desarrollo rollout status deployment/visor
    deployment "visor" successfully rolled out

    NAME                     READY   STATUS    RESTARTS      AGE   IP
    api-54d6dc584c-s4q7b     1/1     Running   0             20s   10.42.0.35
    postgis-0                1/1     Running   15 (176m ago) 21d   10.42.2.166
    visor-7d5d85594d-gmvlh   1/1     Running   0             20s   10.42.0.36

    $ python infra/verificar_cd.py --entorno desarrollo

      ok    hay pods en el namespace  3 pods
      ok    api-54d6dc584c-s4q7b esta Ready
      ok    postgis-0 esta Ready
      ok    visor-7d5d85594d-gmvlh esta Ready
      ok    estan los dos Deployment  ['api', 'visor']
      ok    la API responde 200 en /salud desde el cluster  200

    6 de 6 comprobaciones

**La ultima es la que vale**: los pods pueden estar `Ready` porque sus sondas
dicen que si. Esa linea pregunta de verdad, desde dentro del cluster, y devolvio
200.

Se ve tambien que el `rollout status` de la API **espero**: «0 of 1 updated
replicas are available» y despues «successfully rolled out». Sin esa espera,
`kubectl apply` habria devuelto exito con la API todavia levantando.

### Dos cosas que salieron de correrlo, y no de escribirlo

**Las imagenes de ghcr.io se bajan sin credenciales.** Los pods arrancaron antes
de importarlas a mano, asi que los paquetes son accesibles. El
`k3d image import` de la accion compuesta se conserva igual: en el runner evita
depender de la red en el momento del despliegue, y hace que un fallo de descarga
se vea como fallo de descarga y no como `ImagePullBackOff`.

**El `postgis-0` que ya estaba corriendo se reconfiguro, no se recreo.**
`statefulset.apps/postgis configured` sobre un pod de 21 dias: la base sobrevive
al despliegue, que es el comportamiento que se queria.

---

## Las decisiones, y que descarta cada una

### El despliegue no construye las imagenes

Consume las que H11.1 publico, por SHA exacto. Reconstruirlas al desplegar
produciria un binario distinto del que se probo, y el despliegue dejaria de
decir algo sobre lo verificado.

Se bajan con `docker pull` y se importan al cluster con `k3d image import`, en
vez de dejar que Kubernetes las baje. Evita crear un `imagePullSecret` para un
registro privado, y hace que un fallo de red se vea como un fallo de descarga y
no como un pod en `ImagePullBackOff`, que se lee como un problema del
manifiesto.

### Ningun despliegue automatico queda en `latest`

La base declara `latest` para poder aplicarse a mano sin herramientas. El flujo
la reemplaza por el SHA del commit, y **el verificador comprueba que lo aplicado
no diga `latest`**.

Con `latest`, dos corridas del mismo manifiesto dan resultados distintos y nada
registra cual esta corriendo.

### La reversion de H11.4 se PROVOCA, no se declara

Es la parte que mas costaba hacer verificable.

Produccion despliega **dos veces a proposito**: primero `latest`, que establece
la revision 1, y despues el SHA del commit, que produce la revision 2. Sin el
primer paso no hay a que revertir, y `kubectl rollout undo` sobre una revision
unica **no hace nada mientras informa exito** — seria una reversion que no puede
fallar, o sea ninguna.

Y con el disparo manual `probar_reversion` se despliega una etiqueta que **no
existe**: el rollout no converge, la reversion entra, y se comprueba que el
sistema vuelve a la revision anterior. Es la unica manera de saber que el camino
de reversion funciona **antes** de necesitarlo.

Esto es una ventaja del cluster efimero que el persistente no tiene: provocar un
fallo para probar la reversion en un entorno real significa romper el entorno de
alguien.

### El resultado del trabajo sigue al despliegue, no a la reversion

Si el despliegue falla y la reversion funciona, el trabajo **sale en rojo igual**.

Sin ese paso, un despliegue roto saldria en verde por haberse revertido bien, y
nadie se enteraria de que produccion quedo en la version vieja. Una reversion
exitosa es una red que funciono, no un despliegue exitoso.

### La aprobacion se comprueba contra la API de GitHub

H11.3 y H11.4 piden aprobacion manual y explicita. Eso **no vive en este
repositorio**: son `environment:` con revisores, configurados en Settings.

Un flujo puede decir `environment: produccion` y salir verde aunque ese entorno
no exista o no tenga revisores, y entonces **la historia esta incumplida con el
CI en verde**. Es la forma de **I-25**.

`verificar_cd.py --comprobar-aprobacion` consulta la API y falla si el entorno
no tiene revisores. Es la unica manera de que una condicion que vive fuera del
repositorio quede vigilada por una maquina.

### La comprobacion estatica corre en el CI, no solo en el CD

`cd.yml` solo corre sobre `main`. Sin el modo `--manifiestos` en `ci.yml`, un
manifiesto roto se fusionaria a `dev`, de ahi a `main`, y recien alli se
descubriria — con el despliegue como primer aviso.

---

## El flujo corrio entero, y con la reversion provocada

**Corrida `CD #4`, disparo manual con `probar_reversion=true`, 2026-09-03,
18 m 27 s, los cuatro trabajos en verde.** Commit `8bb6849` sobre `main`.

Las dos aprobaciones quedaron registradas en la propia corrida: `pruebas` a las
03:27 y `produccion` a las 03:29, las dos por HumanoidCat.

    Que version se despliega            2 s
    Desplegar a desarrollo (H11.2)      2 m  5 s     8 de 8 comprobaciones
    Desplegar a pruebas (H11.3)         2 m  4 s     8 de 8  +  2 de 2 aprobacion
    Desplegar a produccion (H11.4)      6 m  4 s     8 de 8 tras la reversion

### La reversion, paso por paso

    03:29:51  Establecer la revision anterior   despliega `latest`, converge
              deployment "api" successfully rolled out
              deployment "visor" successfully rolled out

    03:30:19  Desplegar la version nueva        etiqueta inexistente, a proposito
              deployment.apps/api configured
              deployment.apps/visor configured

              ... cuatro minutos sin converger ...

    03:34:20  Revertir si no convergio
              ##[error]El despliegue no convergio. Revirtiendo a la revision anterior.
              deployment.apps/api rolled back
              deployment.apps/visor rolled back
              deployment "api" successfully rolled out

    03:34:33  python infra/verificar_cd.py --entorno produccion --tras-reversion

              ok    hay pods en el namespace  3 pods
              ok    api-54d6dc584c-dr52m esta Ready
              ok    postgis-0 esta Ready
              ok    visor-7d5d85594d-tcfvb esta Ready
              ok    estan los dos Deployment  ['api', 'visor']
              ok    api volvio a la revision anterior    ghcr.io/humanoidcat/geoguardian/api:latest
              ok    visor volvio a la revision anterior  ghcr.io/humanoidcat/geoguardian/visor:latest
              ok    la API responde 200 en /salud desde el cluster  200

              8 de 8 comprobaciones

**Los cuatro minutos de silencio son el criterio cumpliendose.** El `rollout
status` espero hasta agotar su limite porque la imagen no existia; sin ese
limite, `kubectl apply` habria devuelto exito y la reversion no se habria
disparado nunca.

**Los dos errores rojos en las anotaciones de la corrida son la prueba, no un
fallo.** Son el paso que falla a proposito. El paso que pondria el trabajo en
rojo -«El despliegue fallo, aunque la reversion funcionara»- lleva
`if: inputs.probar_reversion != true` y por eso no corrio.

### Lo que salio de mirar los nombres de los pods, y no estaba previsto

Comparando el `get pods` de antes con el de despues de la reversion:

| | Antes del despliegue malo | Despues de revertir | Estrategia |
|---|---|---|---|
| api | `api-54d6dc584c-2gzjx` | `api-54d6dc584c-`**`dr52m`** | `Recreate` |
| visor | `visor-7d5d85594d-tcfvb` | `visor-7d5d85594d-`**`tcfvb`** | `RollingUpdate` |

**El pod del visor es el mismo.** Nunca se fue.

Con `RollingUpdate` y una replica, el pod viejo no se retira hasta que el nuevo
esta `Ready`, y el nuevo nunca lo estuvo. El `rollout undo` del visor descarto un
ReplicaSet que jamas atendio una peticion: fue **preventivo**, no reparador.

El de la API si cambio. Con `Recreate` -elegido a proposito, porque un rolling
update con una sola replica contra una base con estado dejaria dos procesos
escribiendo a la vez- el pod viejo **se mata antes** de levantar el nuevo. O sea
que durante esos cuatro minutos **la API estuvo caida de verdad**, y la reversion
la devolvio.

**Entonces la corrida demuestra dos cosas distintas, no una:**

- Para la **API**, que la reversion **restablece el servicio** despues de una
  caida real. Es la prueba fuerte.
- Para el **visor**, que un despliegue malo **no alcanza a tumbarlo**. Es una
  garantia mejor, pero es otra, y no prueba lo mismo.

No estaba previsto y no cambia ninguna decision: las dos estrategias siguen
siendo las correctas por los motivos que ya estaban escritos. Queda anotado
porque **decir «se probo la reversion» a secas seria mas de lo que la corrida
sostiene** para el visor.

---

## Lo que esto NO demuestra

**Que quede algo corriendo.** El despliegue automatico demuestra que el
despliegue funciona; no deja un sistema en linea.

**Que el cluster persistente este al dia.** Se actualiza cuando una persona
corre el guion. No es entrega continua hasta el entorno persistente, y llamarlo
asi seria mentir en la memoria.

Las dos van a **limitaciones del documento IEEE**, no a una nota al pie.

### Dos historias de otros quedan condicionadas, y se les avisa hoy

**H13.2, el manual de operacion de Avril**, documenta un sistema que se levanta
localmente, no uno con una direccion publica permanente. Se le avisa el
2026-09-02, no al entregarlo.

**H12.2, la pantalla de monitoreo de entornos**, no puede leer estado de un
cluster que se destruye. Va a tener que leer del ultimo despliegue local o del
historial de corridas, y eso cambia su diseno.
